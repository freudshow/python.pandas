replicate_app

This small app reads an Excel workbook and replicates rows 1..773 into 11 variants each.

Install:

```bash
pip install -r requirements.txt
```

Run:

```bash
python replicate_app.py data.xlsx data_replicated.xlsx Sheet1
```

Defaults: `data.xlsx`, `data_replicated.xlsx`, `Sheet1`.
